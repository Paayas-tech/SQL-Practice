# Subqueries

A subquery is a query inside another SQL query. It allows one query to use the result of another query.

## Topics Covered

- First Subquery
- Correlated Subquery
- Self-Correlated Subquery
- Subqueries in WHERE
- EXISTS
- Combining Queries with UNION